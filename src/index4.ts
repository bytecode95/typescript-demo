//tuples  -- which is a fixed length array whwre each element as partuclar type
//we often use this when working with pair of values
//when we have use present user with two variables id & name key value pairs

let user: [number, string] = [1, 'Mosh'];
